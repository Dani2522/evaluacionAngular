import { TituloEnMayusculaPipe } from './titulo-en-mayuscula.pipe';

describe('TituloEnMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new TituloEnMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
