import { OrdenarPorTituloPipe } from './ordenar-por-titulo.pipe';

describe('OrdenarPorTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
